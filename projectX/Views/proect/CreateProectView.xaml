<UserControl x:Class="projectX.Views.proect.CreateProectView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:projectX.Views.proect"
             xmlns:mv="clr-namespace:projectX.ViewModel.proectVM"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">

	<UserControl.DataContext>
		<mv:CreateProectViewModel/>
	</UserControl.DataContext>

	<Grid Background="AliceBlue">
		<ScrollViewer VerticalScrollBarVisibility="Auto">
			<StackPanel >

				<TextBlock Text="Новый элемент" Margin="10"/>
				<Button Content="Save" Command="{Binding Path=SaveCaseCommnad}" >
					<Button.Style>
						<Style>
							<Style.Triggers>
								<DataTrigger Binding="{Binding ElementName=TBName, Path=Text.Length, Mode=OneWay}" Value="0">
									<Setter Property="Button.IsEnabled" Value="False"/>
								</DataTrigger>
								<DataTrigger Binding="{Binding ElementName=TBDescription, Path=Text.Length, Mode=OneWay}" Value="0">
									<Setter Property="Button.IsEnabled" Value="False"/>
								</DataTrigger>
							</Style.Triggers>
						</Style>
					</Button.Style>
				</Button>
				<TextBlock Text="Новый элемент"/>
				<TextBlock Text="Имя"/>
				<TextBox Text="{Binding Path=NewProect.Name}" Name="TBName"/>
				<TextBlock Text="Описание"/>
				<TextBox Text="{Binding Path=NewProect.Description}" TextWrapping="Wrap" Name="TBDescription" AcceptsReturn="True"/>

				<TextBlock Text="Теги"/>
				<ListBox ItemsSource="{Binding Path=Marks}">
					<ListBox.ItemsPanel>
						<ItemsPanelTemplate>
							<WrapPanel Width="{Binding (FrameworkElement.ActualWidth),
									RelativeSource={RelativeSource AncestorType=ScrollContentPresenter}}"
							           ItemWidth="{Binding (ListView.View).ItemWidth,
									RelativeSource={RelativeSource AncestorType=ListView}}"
							           MinWidth="{Binding ItemWidth, RelativeSource={RelativeSource Self}}"
							           ItemHeight="{Binding (ListView.View).ItemHeight, 
						            RelativeSource={RelativeSource AncestorType=ListView}}"/>
						</ItemsPanelTemplate>
					</ListBox.ItemsPanel>
					<ListBox.ItemTemplate>
						<DataTemplate>
							<StackPanel>
								<TextBlock Text="{Binding Path=Text}" Margin="5 0 0 0"/>
								<Button Content="x"  
								        Command="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType={x:Type UserControl}}, Path=DataContext.DeleteMarkCommnad}"
								        CommandParameter="{Binding}"/>
							</StackPanel>
						</DataTemplate>
					</ListBox.ItemTemplate>
				</ListBox>
				<TextBox Text="{Binding NewMark, UpdateSourceTrigger=PropertyChanged}"/>

				<StackPanel Orientation="Horizontal">
					<Button Content="Add mark" Command="{Binding Path=AddMarkCommnad}"/>
				</StackPanel>

			</StackPanel>
		</ScrollViewer>
	</Grid>
</UserControl>
